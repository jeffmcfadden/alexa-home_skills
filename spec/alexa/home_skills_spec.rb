require 'spec_helper'

describe Alexa::HomeSkills do
  it 'has a version number' do
    expect(Alexa::HomeSkills::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
